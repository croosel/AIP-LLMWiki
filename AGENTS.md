# AIP-LLMWiki Knowledge Base — Agent Mandatory Rules
> **THIS FILE IS READ BY ALL AGENTS. DO NOT SKIP ANY RULE.**
> 
> 本文件是知识库的强制执行规则。任何 Agent 在此知识库中执行任何任务前，必须遵守以下规则。
> 违反规则的产出将被回滚。
---
## 0. 启动检查（每次任务前必做）
```
□ 读取 wiki/经验索引.md，检查是否有与当前任务相关的历史经验
□ 如有相关失败复盘（outcome: failure），必须确认当前方案已规避已知陷阱
□ 如有 verified: true 的实验结论，优先采用已验证方案
□ 🆕 6/21 增：阅读 wiki/苏格拉底老道.md + wiki/多Agent并写守门规则.md 复述三原则
```
## 1. 目录权限
| 目录 | 权限 | 说明 |
|------|------|------|
| `wiki/` | 读写 | 结构化知识页面，所有 Agent 的产出写入此目录 |
| `raw/` | **只读** | 原始资料，禁止任何 Agent 修改 |
| `schema/` | 受控 | 仅 Human Editor 可修改 |
| `.obsidian/` | 禁止 | 不要碰 Obsidian 配置 |
## 2. 页面创建规范
- 必须有 YAML frontmatter：`tags`, `created`, `outcome`（经验记录时）
- 必须使用 `[[wikilinks]]` 链接相关页面（不用 `.md` 后缀，不用 `\\` 转义）
- 新页面至少链接 2 个已有页面
- 创建前检查是否已有同名或同义页面
## 3. 经验反馈闭环（强制）
**执行前**：查 `wiki/经验索引.md`，读取相关经验页面
**执行后**：按 `wiki/经验记录模板.md` 记录本次经验（除非是纯读取/格式修复任务），并更新经验索引
**验证等级**：
- `verified: true` → 可直接作为决策依据
- `confidence: high` → 可作为主要参考
- `confidence: medium` → 仅供参考，需自行验证
- `confidence: low` → 不建议直接引用
## 4. Agent 交接
任务完成需交接时，按 `wiki/Agent-Handoff协议.md` 格式传递信息。交接前确认经验记录已归档。
## 5. 多 Agent 协作守门规则（v1.1 增 · 2026-06-21）

> **来源**：主人 12:11 拍板从草案升正式生效版 v1，12:25 升级 v1.1（5 补丁）
> **约束对象**：所有 Agent（老道 / Qoder / 未来 N+1 Agent）
> **详细规则**：`wiki/多Agent并写守门规则.md`

### 5.1 3 层守门（必读）

| 层 | 名称 | 规则 |
|----|------|------|
| **层 1** | 粒度拆分 | 每个 Agent 只在自己负责的子目录/页面写；禁止两个 Agent 同时写同一文件 |
| **层 2** | 写入前必查 | 任何写操作前必须 read_file 拉最新版；>100 行页面写前先 cp 到 wiki/.bak/ |
| **层 3** | 写后必登记 | 每次写完在 `wiki/经验索引` 登记 + 同步 `wiki/Agent工作流协议` 近期写入表 |

### 5.2 派发状态机（v1.1 增）

| 状态 | 触发 | 动作 |
|------|------|------|
| 待命 | 默认 | 不读不写，等老道派发 |
| 派发中 | 老道派发"任务三件套" | 读 /AGENTS §5 + 守门规则 + 派发页面 + 相关经验 4 份 |
| 执行中 | 收到老道开工指令 | 按派发检查清单 5 项执行 |
| 报告 | 写完 | 主动报告 + 老道验证 |
| 越界 | 写入超出派发范围 | 老道立即叫停，写 EXP-* 经验 |

### 5.3 Agent 类型标注（v1.1 增）

所有派发消息必带 Agent 类型：

| 类型 | 特点 | 守门规则 |
|------|------|----------|
| 执行型（Qoder）| 按指令落地 | 严格守门，禁自主扩大 |
| 建议型 | 提方案不写 | 不触发 3 层守门 |
| 混合型 | 复杂任务 | 按场景细化 |

### 5.4 写入锁（v1.1 增，高强度场景）

- 写时创建 `.lock` 隐藏文件
- 其他 Agent 看到 `.lock` 跳过该文件，等 30 秒后再读
- 写完删除 `.lock`
- 适用：>500 行的长页面 / Agent-Handoff 接力 / Schema 文件

### 5.5 违规处理

| 违规类型 | 处理 |
|----------|------|
| 违反层 1 | 立即停止；老道先写完 → 对方 Agent 读最新版再写 |
| 违反层 2 | 回滚到 .bak/，重新 read + write |
| 违反层 3 | 补登记 + 写入近期写入表 |
| 重复违反 | 写 EXP-* + 通知主人 |

## 6. 详细协议索引
| 协议 | 路径 | 用途 |
|------|------|------|
| Agent 工作流协议 | `wiki/Agent工作流协议.md` | 完整工作规范 |
| Handoff 协议 | `wiki/Agent-Handoff协议.md` | Agent 间交接 |
| 维护 SOP | `wiki/知识库维护SOP.md` | 日/周/月维护 |
| 经验索引 | `wiki/经验索引.md` | 经验查询入口 |
| 经验模板 | `wiki/经验记录模板.md` | 记录标准格式 |
| **多 Agent 守门规则** | `wiki/多Agent并写守门规则.md` | **3 层守门 + 派发状态机（v1.1）** |
| **苏格拉底老道** | `wiki/苏格拉底老道.md` | **思考原则 v1.2 规范版** |
| **经验累积 SOP** | `wiki/经验累积SOP-20260621.md` | **5 步累积链路** |
| **Qoder 知识写入风格** | `wiki/Qoder知识写入风格-20260621.md` | **6 大板块 + 8 项自检** |
| **Agent 接入指引** | `wiki/Agent接入指引-多Agent并写-20260621.md` | **新 Agent 接入清单** |
| **守门规则提示词** | `wiki/多Agent并写守门规则-可复制提示词-20260621.md` | **可复制 system prompt 片段** |

---
*Last updated: 2026-06-21 | Managed by Human Editor + 老道*
*版本：v1.1（增 §5 多 Agent 协作守门规则，76+ 行）*
